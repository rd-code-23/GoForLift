// Presents the dashboard heading inside the shared responsive application shell.
import { SectionLanding } from './section-landing';

export function DashboardLanding() {
  return <SectionLanding title="Dashboard" />;
}
